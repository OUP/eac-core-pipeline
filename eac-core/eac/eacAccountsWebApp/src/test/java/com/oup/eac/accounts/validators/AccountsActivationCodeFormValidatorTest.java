package com.oup.eac.accounts.validators;

import javax.naming.NamingException;

import org.easymock.EasyMock;
import org.junit.Test;

import com.oup.eac.accounts.dto.CodeRedeemDto;

public class AccountsActivationCodeFormValidatorTest /* extends AbstractValidatorTest */ {
	/*
	 * 
	 * private AccountsActivationCodeFormValidator validator;
	 * 
	 * public AccountsActivationCodeFormValidatorTest() throws NamingException {
	 * super(); }
	 * 
	 * @Override public void setUp() { super.setUp(); validator = new
	 * AccountsActivationCodeFormValidator(); }
	 * 
	 * @Test public void testBlankCode() { CodeRedeemDto codeRedeemDto = new
	 * CodeRedeemDto();
	 * 
	 * errors.rejectValue(EasyMock.eq("code"), EasyMock.eq("error.not-specified"),
	 * eqLabels(new Object[] { "label.redeemcode" }),
	 * EasyMock.eq("Activation Code is required."));
	 * 
	 * replayMocks();
	 * 
	 * validator.validate(codeRedeemDto, errors);
	 * 
	 * verifyMocks(); }
	 */}
