package com.oup.eac.web.validators.login;

import javax.naming.NamingException;

import org.easymock.EasyMock;
import org.junit.Test;

import com.oup.eac.dto.PasswordResetDto;

public class PassswordResetFormValidatorTest /* extends AbstractValidatorTest */ {
	/*
	 * 
	 * private PasswordResetFormValidator validator;
	 * 
	 * public PassswordResetFormValidatorTest() throws NamingException { super(); }
	 * 
	 * @Override public void setUp() { super.setUp(); validator = new
	 * PasswordResetFormValidator(); }
	 * 
	 * @Test public void testNoUsername() { final PasswordResetDto passwordResetDto
	 * = new PasswordResetDto();
	 * 
	 * errors.rejectValue(EasyMock.eq("username"),
	 * EasyMock.eq("error.not-specified"), eqLabels(new Object[] { "label.username"
	 * }), EasyMock.eq("Username " + "is required."));
	 * 
	 * replayMocks();
	 * 
	 * validator.validate(passwordResetDto, errors);
	 * 
	 * verifyMocks(); }
	 * 
	 * @Test public void testValidateWhenEmailAddress() { final PasswordResetDto
	 * passwordResetDto = new PasswordResetDto();
	 * passwordResetDto.setUsername("test@test.com");
	 * 
	 * replayMocks();
	 * 
	 * validator.validate(passwordResetDto, errors);
	 * 
	 * verifyMocks(); }
	 * 
	 * @Test public void testValidateWhenUsername() { final PasswordResetDto
	 * passwordResetDto = new PasswordResetDto();
	 * passwordResetDto.setUsername("smithj");
	 * 
	 * replayMocks();
	 * 
	 * validator.validate(passwordResetDto, errors);
	 * 
	 * verifyMocks(); }
	 */}
