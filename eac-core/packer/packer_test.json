{
  "builders": [
    {
      "associate_public_ip_address": "false",
      "ami_name": "EAC APP Build Test",
      "instance_type": "t3.micro",
      "name": "CodePipeline EAC AMI Builder",
      "region": "{{user `aws_region`}}",
      "source_ami": "{{user `ami_id`}}",
      "ssh_username": "ec2-user",
      "subnet_id": "{{user `subnet`}}",
      "type": "amazon-ebs",
      "vpc_id": "{{user `vpc`}}",
      "skip_create_ami": true
    }
  ],
  "provisioners": [
    {
      "type": "ansible",
      "use_proxy": false,
      "playbook_file": "{{user `playbook_path`}}/playbook-ami-test.yml"
    }
  ],
  "variables": {
    "ami_name": "EAC-CorePipeline-AMI-{{env `ENVIRONMENT`}}-{{isotime \"02-Jan-06 03_04_05\"}}",
    "environment": "{{env `ENVIRONMENT`}}",
    "subnet": "{{env `BUILD_SUBNET_ID`}}",
    "vpc": "{{env `BUILD_VPC_ID`}}",
    "ami_id": "{{env `AMI_ID`}}",
    "playbook_path": "{{env `CODEBUILD_SRC_DIR`}}/eac-core/ansible"
  }
}
