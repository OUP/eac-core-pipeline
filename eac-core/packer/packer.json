{
  "builders": [
    {
      "ami_description": "EAC AMI",
      "ami_name": "{{user `ami_name` | clean_resource_name}}",
      "associate_public_ip_address": "false",
      "instance_type": "t3.micro",
      "name": "CodePipeline EAC AMI Builder",
      "region": "{{user `aws_region`}}",
      "run_tags": {
        "Name": "EAC_AMI_test"
      },
      "run_volume_tags": {
        "Name": "EAC_AMI_test"
      },
      "snapshot_tags": {
        "Name": "EAC_AMI_test"
      },
      "source_ami": "ami-0ba539fe92fb117fe",
      "ssh_username": "ec2-user",
      "subnet_id": "{{user `subnet`}}",
      "type": "amazon-ebs",
      "vpc_id": "{{user `vpc`}}"
    }
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "build/",
      "destination": "/tmp/target"
    }
  ],
  "variables": {
    "ami_name": "EAC-CorePipeline-AMI-{{isotime \"02-Jan-06 03_04_05\"}}",
    "subnet": "{{env `BUILD_SUBNET_ID`}}",
    "vpc": "{{env `BUILD_VPC_ID`}}"
  }
}
